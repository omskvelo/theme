<ips:template parameters="$url, $newWindow=TRUE, $title=NULL, $wordbreak=TRUE" />
{{if $wordbreak}}<div class='ipsType_break ipsContained'>{{endif}}<a href='{$url}'{{if $newWindow === TRUE}} target='_blank'{{endif}}>{{if $title}}{$title}{{else}}{$url}{{endif}}</a>{{if $wordbreak}}</div>{{endif}}